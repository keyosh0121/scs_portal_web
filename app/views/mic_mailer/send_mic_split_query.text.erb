<%= @mic.band.name if @mic.band%>
バンマス  <%= @mic.band.master.name if @mic.band.master%>様
<%= @mic.date.strftime("%m月%d日" + "(#{%w(日 月 火 水 木 金 土)[@mic.date.wday]})") %>、同じ時限に申請されているマイク練が複数あるため、分割希望時間帯の申請をしてください。

【現在の申請状況(申請順)】
<% @mics.order(:created_at).each_with_index do |mic,i|%>
  <%= i + 1%>.<%= mic.band.name %>
<%end%>


以下のリンクをクリックして、申請画面から申請を行ってください。

<%= link_to 'こちらから分割希望の申請を行ってください',"scsportal.herokuapp.com/mic-order-registration/#{@mic.id}" %>

